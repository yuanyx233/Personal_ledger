## 1. 数据与 API

- [x] 1.1 实现预算校验、D1 生效月份查询与受保护写入，并验证跨月修改、取消和无效请求。
- [x] 1.2 完整备份升级 v3，验证预算历史恢复及旧备份兼容。

## 2. 界面与验证

- [x] 2.1 月度分析增加板块占比和预算编辑/剩余/超支状态，覆盖空月份、币种与账户筛选。
- [x] 2.2 验证浏览器桌面/手机交互、格式、类型、lint、相关回归与构建，记录证据。

## 验证记录（2026-09-05）

- 单元覆盖率运行：30 文件、256 测试通过，statement 91.38%、branch 85.36%，满足已有门槛。
- Worker 集成覆盖率：21 文件、131 测试通过，statement 91.63%、branch 86.76%，满足已有门槛。
- 浏览器全套：58 通过、2 个已有移动端重复场景跳过；预算新增流程桌面和手机均通过，空月在 320/768/1024/1440 宽度无页面横向溢出，成功流程无浏览器 warning/error。
- 独立临时数据库 JSON 恢复验证通过，精确核对 CAD 正数、USD 零预算和取消记录；v1/v2 兼容及重复键/缺失类别拒绝由单元测试覆盖。
- format:check、lint、typecheck、生产构建、git diff --check 与 OpenSpec strict 校验通过。
- 桌面及手机 Goal 区截图已人工视觉检查，位于 test-results/analysis-sets-monthly-cate-60789--budget-and-currency-states-chromium-{desktop,mobile}/monthly-budgets.png，截图使用合成测试数据。
- 构建最初遇到沙箱日志目录写入限制，已将 WRANGLER_LOG_PATH 设为 /tmp 后复验。构建仍提示本地未配置登录密钥；这不阻止打包，身份访问由测试凭据在 Worker 集成中验证。
- 自查确认预算复用既有净支出统计、金额未跨币种混加、写入复合键不会覆盖更早月份、取消记录包含在备份中。没有提交、远端迁移或部署。

## 3. 用户后续授权：上线及 UofT 学费分类

用户随后明确要求“上线吧，然后添加一个新的分类，那些给uoft的转账是我的学费”。本次授权包括现有账本的必要迁移、预算功能发布、创建学费支出分类和校正已核对的 UofT 付款及对应 exact 商户规则。

- [x] 3.1 核对现有 App/D1 和回退版本；重新运行 npm run verify，保存完整线上 JSON 并在独立本地目录验证恢复，审查待应用迁移。
- [x] 3.2 应用 0018/0019 迁移并发布固定的已验证构建。
- [x] 3.3 新增学费分类，精确校正 4 笔 UofT 付款及 4 条对应商户规则，核对金额、审计和未受影响记录。
- [x] 3.4 验证线上月度预算、学费分类、匿名访问保护和部署后备份，记录发布版本。

### 授权上线结果

- 发布版本 `55b923ad-3b2f-4ca5-b1e5-5acbb6c8293b`，原版本 `52e07c17-4822-4059-b435-9d458f82bdab` 可用于兼容回退；原 D1 保留。
- 固定构建位于 `/var/folders/2v/r5fc_0g1441dpg39f380yksc0000gn/T/ledger-budget-release.yv78tb2y`，Worker SHA-256 `2bb66db72d82c80f6fb364c5465f0cbd5c64d6b8f61b6b0a984bc4f824c09e83`。Wrangler dry-run 和正式上传均成功。
- 全套 `npm run verify` 再次通过；生产依赖 audit 为 0 漏洞。D1 复核只有待执行的 0018/0019 被新增，外键违规 0。
- 学费类别是活跃 EXPENSE；4 笔已核对付款改为 MANUAL 学费，3 条新 exact 规则和 1 条原规则更新；自动追加 4 条分类审计。原始金额、币种、方向、日期不变。
- 前后完整 JSON 比对确认其余 415 笔交易、已有分类及无关规则、导入与历史转账记录保持不变。快照和逐项校验结果保存在被忽略的 `backups/2026-09-05-budget-tuition/`。
- 迁移前及完成后的备份都已在独立本地目录验证恢复。完整交易数仍为 419；类别 16，规则 111，分类审计 489。生产没有创建测试交易或测试预算。
- 已登录浏览器的 2026-07 分析页加载 Goal 区和学费统计，学费预算编辑框可打开和关闭，日志无 warning/error。匿名 `/analysis` 返回 302；Access 设置未修改。
- 未执行 Git 提交、推送，也未删除旧 Worker 或旧密钥。
