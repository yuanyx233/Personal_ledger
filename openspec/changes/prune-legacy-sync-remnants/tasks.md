## 1. Export format reduction

- [x] 1.1 Update the failing domain export tests first, then remove connection, account, transfer-match, transfer-match-audit, and Plaid classification records from the full JSON export schema and serializer.
- [x] 1.2 Update the failing persistence tests first, then remove the same record sets from export population, restore SQL generation, and restore evidence.
- [x] 1.3 Update the failing Worker export tests first, then confirm the export route and its secret-free guarantees still hold.
- [x] 1.4 Remove the legacy counts and the transfer-match exclusion clause from `scripts/restore-json-export.mjs` and `scripts/verify-json-restore.mjs`, and remove the now-unused test support helper.

## 2. Superseded category suggestions

- [x] 2.1 Delete the unused post-write confirmation component and its dead stylesheet rules.
- [x] 2.2 Remove the `category-suggestions` route, `CategoryRepository.suggestForTransaction`, their contracts, and their tests, including the browser mock.

## 3. Unreferenced declarations

- [x] 3.1 Remove the Plaid test fixtures and the exported declarations that nothing references.
- [ ] 3.2 Narrow module-internal exports. **Not done, deliberately.** Most candidates are return or parameter types of exported members; dropping `export` would break declaration emit, and the pass removes no lines. Left as-is rather than churning files another change is editing concurrently.

## 4. Verification

- [x] 4.1 Run `npm run verify` and `npm run test:restore`, run `openspec validate --strict`, and record the net reduction.

Verification (2026-09-11):

- `openspec validate prune-legacy-sync-remnants --strict` passed.
- `npm run typecheck` passed with no errors.
- `npm run format:check` passed.
- Unit suite: 301 passed, 0 failed (`vitest run --config vitest.unit.config.ts`).
- Worker integration suite: 144 tests, all passing except two owned by the concurrent `transaction-installments` work in progress (`manual-transactions.spec.ts` installment field, `query-plans.spec.ts` `idx_transactions_installment_group`).
- Browser suite: 82 passed, 2 pre-existing skips.
- `npm run test:restore` passed, including the new assertion that a pre-reduction backup is rejected with `INPUT_SCHEMA_INVALID`.
- `npm run lint` reports 3 errors, all in the concurrent installment test code (`apps/web/test/manual-transactions.spec.ts` `expect.any(String)`).
- `npm run test:coverage` is below its global thresholds (86.08% statements, 81.5% branches). The dominant uncovered block is `packages/persistence/src/manual-transactions.ts` at 61.93%, which grew from 425 to 654 lines under the concurrent installment work and has no unit coverage for the new paths.

Reduction against the pre-change snapshot: 52 tracked files changed, 1,198 insertions and 2,694 deletions, net 1,496 lines removed. Two whole files deleted (`apps/web/src/features/quick-entry/CategoryConfirmation.tsx`, and the `category-suggestions` route with its persistence support). Untracked build leftovers removed from disk: `packages/plaid/`, `workers/`, `coverage/`, `test-results/`, and the stale `packages/*/dist` and `apps/web/dist` outputs (regenerated by `npm run build:packages`). Historical migrations, existing D1 tables and rows, and the backups under `backups/` and `.wrangler/` were not touched. No migration, commit, deployment, or remote database change was performed.
